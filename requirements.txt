# LOCKED dependency set. Do not hand-edit — edit requirements.in instead.
#
# Every version below was captured with `pip freeze` from the running container
# on 2026-08-15, so this file reproduces a dependency set observed working, not a
# fresh resolution against whatever PyPI happens to hold today.
#
# Why this file exists: the direct dependencies used to be unpinned `>=` floors.
# The container ran an image built 8 weeks earlier, so nobody noticed the drift
# until a rebuild resolved `mcp[cli]>=1.0.0` to mcp 2.0.0 — which moved FastMCP
# off `mcp.server.fastmcp` — and the container crash-looped. Pinned floors make a
# rebuild reproducible instead of an uncontrolled upgrade.
#
# To upgrade deliberately:
#   pip install pip-tools && pip-compile requirements.in -o requirements.txt
# then rebuild, verify the container starts, and commit the result.

aiofiles==25.1.0
aiohappyeyeballs==2.7.1
aiohttp==3.14.3
aiosignal==1.4.0
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
attrs==26.1.0
certifi==2026.7.22
cffi==2.1.1
click==8.4.2
cryptography==50.0.0
frozenlist==1.8.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
httpx-sse==0.4.3
idna==3.18
iniconfig==2.3.0
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
mac-vendor-lookup==0.1.15
markdown-it-py==4.2.0
mcp==1.29.0
mdurl==0.1.2
miniupnpc==2.3.3
multidict==6.7.1
packaging==26.3
pluggy==1.6.0
propcache==0.5.2
pycparser==3.0
pycryptodome==3.23.0
pydantic==2.13.4
pydantic-settings==2.15.0
pydantic_core==2.46.4
Pygments==2.20.0
PyJWT==2.13.0
pytest==9.1.1
pytest-asyncio==1.4.0
python-dotenv==1.2.2
python-multipart==0.0.32
referencing==0.37.0
respx==0.23.1
rich==15.0.0
rpds-py==2026.6.3
shellingham==1.5.4
speedtest-cli==2.1.3
sse-starlette==3.4.8
starlette==1.6.0
typer==0.27.1
typing-inspection==0.4.4
typing_extensions==4.16.0
uvicorn==0.52.3
yarl==1.24.5
